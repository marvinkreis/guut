Experiment Result:

Test on correct code:
```
```

Test on mutant:
```
```

Debugger on correct code:
```
> test.py(1)<module>()
-> from sieve import sieve
(Pdb) b sieve.py:15
Breakpoint 1 at sieve.py:15
(Pdb) c
> sieve.py(15)sieve()
-> primes.append(n)
(Pdb)
The program exited.
```

Debugger on mutant:
```
> test.py(1)<module>()
-> from sieve import sieve
(Pdb) b sieve.py:15
Breakpoint 1 at sieve.py:15
(Pdb) c
The program exited.
```

Conclusion:
